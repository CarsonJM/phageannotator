process {
    withName: NUCMER {
        ext.args = params.nucmer_args ? params.nucmer_args : ""
        publishDir = [
            path: { "${params.outdir}/ANICalculation/nucmer" },
            mode: params.publish_dir_mode,
            pattern: '*.coords.tsv',
        ]
    }
}