process {
    withName: SKANI_TRIANGLE {
        ext.args = params.skani_args ? params.skani_args : ""
        publishDir = [
            path: { "${params.outdir}/ANICalculation/skani/triangle" },
            mode: params.publish_dir_mode,
            pattern: '*.tsv',
        ]
    }
}
