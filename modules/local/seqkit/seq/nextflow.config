process {
    withName: SEQKIT_SEQ {
        ext.args = params.seqkit_seq_args ? params.seqkit_seq_args : ""
        publishDir = [
            path: { "${params.outdir}/AssemblyFilter/seqkit/seq" },
            mode: params.publish_dir_mode,
            pattern: '*.fast*',
        ]
    }
}
